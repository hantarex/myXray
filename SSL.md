# Установка SSL сертификатов для 3X-UI

Подробное руководство по установке и настройке SSL сертификатов Let's Encrypt для админ-панели 3X-UI с использованием acme.sh.

## Содержание

1. [Введение](#введение)
2. [Быстрый старт](#быстрый-старт)
3. [Подробная инструкция](#подробная-инструкция)
4. [Ручная установка](#ручная-установка)
5. [Настройка панели 3X-UI](#настройка-панели-3x-ui)
6. [Автоматическое обновление](#автоматическое-обновление)
7. [Альтернативные методы](#альтернативные-методы)
8. [Использование сертификатов в Inbounds](#использование-сертификатов-в-inbounds)
9. [Устранение неполадок](#устранение-неполадок)
10. [Безопасность](#безопасность)
11. [Примеры команд](#примеры-команд)

---

## Введение

### Зачем нужен SSL для 3X-UI?

SSL сертификаты обеспечивают:
- **Безопасное подключение** к админ-панели (шифрование HTTPS)
- **Защиту данных** при передаче (логин, пароль, конфигурации)
- **TLS для VPN протоколов** (VLESS, VMess, Trojan)
- **Маскировку трафика** под обычный HTTPS

### Варианты установки сертификатов

1. **Автоматическая через acme.sh** (рекомендуется) - используется в этом руководстве
2. **Встроенная в 3X-UI** - простая, но менее гибкая
3. **Вручную через Certbot** - для продвинутых пользователей
4. **Собственные сертификаты** - от платных CA или self-signed

В этом руководстве мы используем **acme.sh** - это универсальный ACME клиент, который:
- Автоматически получает сертификаты от Let's Encrypt
- Настраивает автоматическое обновление
- Поддерживает множество DNS провайдеров
- Работает на любой Linux системе

---

## Быстрый старт

Для тех, кто хочет быстро установить SSL:

### Шаг 1: Настройте DNS

Убедитесь, что ваш домен указывает на IP сервера:

```bash
# Проверка DNS
nslookup your-domain.com
# Должен показать IP вашего сервера
```

### Шаг 2: Настройте .env

Откройте `.env` файл и заполните:

```bash
DOMAIN=your-domain.com         # Ваш домен
EMAIL=your-email@example.com   # Ваш email
```

### Шаг 3: Запустите скрипт

```bash
cd /home/sham/PhpstormProjects/myXray
sudo ./scripts/setup-ssl.sh
```

Скрипт автоматически:
- Установит acme.sh
- Получит сертификаты
- Настроит auto-renewal
- Скопирует сертификаты в нужное место

### Шаг 4: Настройте панель

1. Откройте: `http://your-domain.com:2053`
2. Войдите в панель
3. Panel Settings → Panel Certificate
4. Укажите пути:
   - Public Key: `/root/cert/fullchain.pem`
   - Private Key: `/root/cert/privkey.pem`
5. Включите HTTPS
6. Сохраните и перезапустите

### Готово!

Откройте: `https://your-domain.com:2053`

---

## Подробная инструкция

### Предварительные требования

#### 1. Домен настроен

Ваш домен должен иметь A-запись, указывающую на IP сервера:

```bash
# Проверка DNS
dig +short your-domain.com
# Должен вернуть IP сервера

# Или через nslookup
nslookup your-domain.com
```

Если DNS не настроен:
1. Зайдите в панель управления доменом (Cloudflare, Namecheap, etc.)
2. Создайте A-запись: `@ → IP_СЕРВЕРА`
3. Подождите 5-10 минут (DNS пропагация)

#### 2. Порты открыты

Необходимые порты:
- **Порт 80** - для верификации домена (HTTP-01 challenge)
- **Порт 2053** - для доступа к админ-панели

Проверка:

```bash
# Проверка что порты слушаются
sudo ss -tulpn | grep -E '(80|2053)'

# Проверка firewall
sudo ufw status
```

#### 3. Docker запущен

```bash
# Проверка Docker
docker --version
docker-compose --version

# Проверка что 3X-UI запущен
cd /home/sham/PhpstormProjects/myXray
docker-compose ps
```

### Пошаговое выполнение

#### Шаг 1: Настройка переменных окружения

Откройте `.env` файл:

```bash
nano .env
```

Найдите и заполните:

```bash
# SSL Configuration
DOMAIN=vpn.example.com           # Замените на ваш домен
EMAIL=admin@example.com          # Замените на ваш email
```

Сохраните: `Ctrl+X`, затем `Y`, затем `Enter`

#### Шаг 2: Запуск скрипта установки

```bash
# Перейдите в директорию проекта
cd /home/sham/PhpstormProjects/myXray

# Запустите скрипт с правами root
sudo ./scripts/setup-ssl.sh
```

#### Шаг 3: Что делает скрипт

Скрипт выполнит следующие действия:

1. **Проверка окружения**
   - Root права
   - Docker установлен
   - .env файл существует

2. **Загрузка конфигурации**
   - Чтение DOMAIN и EMAIL из .env
   - Проверка что домен указывает на сервер

3. **Установка acme.sh**
   - Скачивание и установка acme.sh в ~/.acme.sh/
   - Настройка email для уведомлений

4. **Настройка firewall**
   - Автоматическое определение типа (UFW/firewalld)
   - Открытие порта 80 для ACME верификации

5. **Остановка контейнера**
   - Временная остановка 3x-ui для освобождения портов

6. **Получение сертификата**
   - Запрос сертификата от Let's Encrypt
   - Верификация домена через HTTP-01 challenge

7. **Установка сертификатов**
   - Копирование в data/cert/
   - Настройка прав доступа
   - Настройка auto-renewal

8. **Запуск контейнера**
   - Перезапуск 3x-ui с новыми сертификатами

9. **Проверка**
   - Проверка что сертификаты на месте
   - Проверка cron задачи для обновления

#### Шаг 4: Ожидаемый вывод

```
========================================
  Установка SSL для 3X-UI через acme.sh
========================================

[INFO] Проверка root прав: OK
[INFO] Все зависимости установлены: OK
[INFO] .env файл найден: OK
[INFO] DOMAIN: vpn.example.com
[INFO] EMAIL: admin@example.com
[INFO] IP сервера: 1.2.3.4
[INFO] IP домена: 1.2.3.4
[INFO] DNS настройки корректны: OK
[INFO] acme.sh установлен: OK
[INFO] Порт 80 открыт: OK
[STEP] Остановка контейнера 3x-ui...
[INFO] Контейнер остановлен: OK
[STEP] Получение SSL сертификата...
[INFO] Сертификат получен: OK
[STEP] Установка сертификата в data/cert/...
[INFO] Сертификаты установлены: OK
[INFO] Контейнер запущен: OK
[INFO] Срок действия: May 15 00:00:00 2026 GMT

========================================
  SSL Сертификаты успешно установлены!
========================================
```

---

## Ручная установка

Если вы хотите установить сертификаты вручную или автоматический скрипт не подходит:

### 1. Установка acme.sh

```bash
# Установка
curl https://get.acme.sh | sh -s email=your-email@example.com

# Перезагрузка переменных окружения
source ~/.bashrc

# Или перезайдите в терминал
```

### 2. Остановка контейнера

```bash
cd /home/sham/PhpstormProjects/myXray
docker-compose stop
```

### 3. Получение сертификата

```bash
# Standalone режим
~/.acme.sh/acme.sh --issue --standalone -d your-domain.com

# Если порт 80 занят, используйте webroot
~/.acme.sh/acme.sh --issue --webroot /path/to/webroot -d your-domain.com
```

### 4. Установка сертификата

```bash
# Создание директории
mkdir -p /home/sham/PhpstormProjects/myXray/data/cert

# Установка с auto-renewal
~/.acme.sh/acme.sh --install-cert -d your-domain.com \
  --key-file /home/sham/PhpstormProjects/myXray/data/cert/privkey.pem \
  --fullchain-file /home/sham/PhpstormProjects/myXray/data/cert/fullchain.pem \
  --reloadcmd "cd /home/sham/PhpstormProjects/myXray && docker-compose restart"

# Настройка прав
chmod 644 /home/sham/PhpstormProjects/myXray/data/cert/fullchain.pem
chmod 600 /home/sham/PhpstormProjects/myXray/data/cert/privkey.pem
```

### 5. Запуск контейнера

```bash
cd /home/sham/PhpstormProjects/myXray
docker-compose start
```

---

## Настройка панели 3X-UI

После установки сертификатов нужно включить HTTPS в админ-панели.

### Шаг 1: Вход в панель

Откройте браузер и перейдите:

```
http://your-domain.com:2053
```

Войдите с учётными данными из `.env`:
- Username: значение `PANEL_USERNAME`
- Password: значение `PANEL_PASSWORD`

### Шаг 2: Переход в настройки

1. В левом меню нажмите **"Panel Settings"** (Настройки панели)
2. Или нажмите на иконку шестерёнки в правом верхнем углу

### Шаг 3: Настройка сертификатов

Найдите секцию **"Panel Certificate"** или **"SSL Settings"**

Заполните поля:

```
Panel Certificate Public Key File Path:  /root/cert/fullchain.pem
Panel Certificate Key File Path:         /root/cert/privkey.pem
```

**Важно:** Пути указываются как они видны **внутри контейнера** (`/root/cert/`), не на хосте!

### Шаг 4: Включение HTTPS

Найдите опцию:
- **"Panel URL Root Path"** - можно оставить `/`
- **"Panel Listen IP"** - оставьте пустым или `0.0.0.0`
- **"Panel Port"** - оставьте `2053` или измените на `443`

Если есть чекбокс **"Certificate Path Effective"** или **"Use HTTPS"** - включите его.

### Шаг 5: Сохранение

1. Нажмите **"Save"** внизу страницы
2. Панель предложит перезапуститься - подтвердите
3. Подождите 10-15 секунд

### Шаг 6: Проверка

Откройте в браузере:

```
https://your-domain.com:2053
```

Вы должны увидеть:
- Зелёный замок в адресной строке
- Сертификат от Let's Encrypt (можно проверить кликнув на замок)
- Никаких предупреждений о безопасности

**Если не работает:**
- Проверьте логи: `docker logs 3x-ui --tail 50`
- Убедитесь что пути к сертификатам правильные
- Попробуйте через HTTP вернуться и перепроверить настройки

---

## Автоматическое обновление

Сертификаты Let's Encrypt действительны 90 дней. acme.sh автоматически их обновляет.

### Как это работает

1. **Cron задача**: acme.sh устанавливает задачу в crontab при установке
2. **Ежедневная проверка**: Каждый день в 00:00 проверяются все сертификаты
3. **Обновление за 60 дней**: Если до истечения осталось меньше 60 дней - обновляет
4. **Reload команда**: После обновления выполняется перезапуск контейнера

### Проверка cron задачи

```bash
# Просмотр cron задач
crontab -l | grep acme

# Должно быть что-то типа:
# 0 0 * * * "/root/.acme.sh"/acme.sh --cron --home "/root/.acme.sh"
```

### Проверка статуса сертификата

```bash
# Информация о сертификате
~/.acme.sh/acme.sh --info -d your-domain.com

# Покажет:
# - Дату создания
# - Дату истечения
# - Путь к сертификатам
# - Reload команду
```

### Ручное обновление

Если нужно обновить сертификат вручную:

```bash
# Принудительное обновление
~/.acme.sh/acme.sh --renew -d your-domain.com --force

# Или с логами
~/.acme.sh/acme.sh --renew -d your-domain.com --force --debug
```

### Логи обновления

```bash
# Просмотр логов acme.sh
tail -f ~/.acme.sh/acme.sh.log

# Последние 50 строк
tail -50 ~/.acme.sh/acme.sh.log
```

### Тестирование обновления

```bash
# Тестовое обновление (без реального выпуска)
~/.acme.sh/acme.sh --renew -d your-domain.com --force --test

# Если всё работает, увидите сообщение об успехе
```

---

## Альтернативные методы

### DNS API метод

Если порт 80 занят или недоступен извне, используйте DNS API:

#### Cloudflare

```bash
# Получите API токен в Cloudflare Dashboard
export CF_Token="your-api-token"
export CF_Account_ID="your-account-id"

# Получение сертификата
~/.acme.sh/acme.sh --issue --dns dns_cf -d your-domain.com
```

#### DigitalOcean

```bash
export DO_API_KEY="your-api-key"
~/.acme.sh/acme.sh --issue --dns dns_dgon -d your-domain.com
```

#### Другие провайдеры

acme.sh поддерживает более 100 DNS провайдеров. Полный список:

```bash
~/.acme.sh/acme.sh --list-dns
```

### Webroot метод

Если у вас уже работает веб-сервер на порту 80:

```bash
# Создайте webroot директорию
mkdir -p /var/www/html/.well-known/acme-challenge

# Получите сертификат
~/.acme.sh/acme.sh --issue --webroot /var/www/html -d your-domain.com
```

### Использование Certbot

Альтернатива acme.sh:

```bash
# Установка Certbot
sudo apt install certbot

# Остановите контейнер
docker-compose stop

# Получите сертификат
sudo certbot certonly --standalone -d your-domain.com

# Скопируйте в data/cert
sudo cp /etc/letsencrypt/live/your-domain.com/fullchain.pem data/cert/
sudo cp /etc/letsencrypt/live/your-domain.com/privkey.pem data/cert/
sudo chmod 644 data/cert/fullchain.pem
sudo chmod 600 data/cert/privkey.pem

# Запустите контейнер
docker-compose start
```

---

## Использование сертификатов в Inbounds

Кроме админ-панели, сертификаты можно использовать для VPN протоколов.

### Настройка TLS для VLESS

1. Перейдите в **Inbounds**
2. Создайте или отредактируйте Inbound
3. Настройте:

```
Protocol: VLESS
Port: 443
Network: TCP or WS
Security: TLS

TLS Settings:
  Server Name (SNI): your-domain.com
  Certificate File: /root/cert/fullchain.pem
  Key File: /root/cert/privkey.pem
  ALPN: h2,http/1.1
```

4. Сохраните

Теперь VLESS будет использовать TLS шифрование с вашим сертификатом.

### Настройка Reality

Reality - это новый протокол, который не требует реальных сертификатов, но можно использовать ваш домен:

```
Protocol: VLESS
Port: 443
Security: Reality

Reality Settings:
  Destination: your-domain.com:443
  Server Names: your-domain.com,www.your-domain.com
```

### Настройка VMess + WS + TLS

```
Protocol: VMess
Port: 443
Network: WebSocket
Path: /vmess

Security: TLS
Certificate: /root/cert/fullchain.pem
Key: /root/cert/privkey.pem
```

### Проверка TLS соединения

```bash
# Проверка что TLS работает
openssl s_client -connect your-domain.com:443 -servername your-domain.com

# Должно показать информацию о сертификате и "Verify return code: 0 (ok)"
```

---

## Устранение неполадок

### Проблема: Порт 80 занят

**Симптомы:**
```
[error] Bind for 0.0.0.0:80 failed: port is already used
```

**Решение 1: Освободите порт 80**

```bash
# Найдите процесс на порту 80
sudo lsof -i :80
# или
sudo ss -tulpn | grep :80

# Остановите процесс (например nginx)
sudo systemctl stop nginx

# Получите сертификат
sudo ./scripts/setup-ssl.sh

# Запустите nginx обратно
sudo systemctl start nginx
```

**Решение 2: Используйте DNS API**

См. раздел [Альтернативные методы → DNS API](#dns-api-метод)

### Проблема: DNS не настроен

**Симптомы:**
```
[ERROR] Домен your-domain.com указывает на 1.2.3.4, но сервер имеет IP 5.6.7.8
```

**Решение:**

1. Проверьте DNS:
```bash
nslookup your-domain.com
dig +short your-domain.com
```

2. Если IP не совпадает:
   - Зайдите в панель управления доменом
   - Обновите A-запись: `@ → IP_СЕРВЕРА`
   - Подождите 5-30 минут (DNS пропагация)

3. Проверьте снова:
```bash
dig +short your-domain.com
```

### Проблема: Сертификат не получен

**Симптомы:**
```
[ERROR] Не удалось получить сертификат
```

**Диагностика:**

```bash
# Проверка логов
cat ~/.acme.sh/acme.sh.log | tail -100

# Тест с debug
~/.acme.sh/acme.sh --issue --standalone -d your-domain.com --debug --force
```

**Частые причины:**

1. **Порт 80 недоступен извне:**
```bash
# Проверка с внешнего сервера
curl -I http://your-domain.com
```

2. **Firewall блокирует:**
```bash
# UFW
sudo ufw allow 80/tcp
sudo ufw reload

# firewalld
sudo firewall-cmd --permanent --add-port=80/tcp
sudo firewall-cmd --reload
```

3. **Провайдер блокирует порт 80:**
   - Используйте DNS API метод
   - Или измените порт в настройках провайдера

### Проблема: HTTPS не работает после настройки

**Симптомы:**
- Панель недоступна по HTTPS
- Ошибка "SSL connection error"
- Предупреждение о недоверенном сертификате

**Решение:**

1. **Проверьте что сертификаты на месте:**
```bash
ls -la /home/sham/PhpstormProjects/myXray/data/cert/

# Должны быть:
# fullchain.pem
# privkey.pem
```

2. **Проверьте внутри контейнера:**
```bash
docker exec 3x-ui ls -la /root/cert/
```

3. **Проверьте логи контейнера:**
```bash
docker logs 3x-ui --tail 50

# Ищите ошибки типа:
# "failed to load certificate"
# "permission denied"
```

4. **Проверьте пути в панели:**
   - Должно быть: `/root/cert/fullchain.pem` (не `/home/sham/...`)
   - Пути указываются как внутри контейнера!

5. **Перезапустите контейнер:**
```bash
docker-compose restart
```

6. **Если ничего не помогает:**
   - Зайдите через HTTP: `http://your-domain.com:2053`
   - Отключите HTTPS в настройках
   - Проверьте всё заново
   - Включите HTTPS снова

### Проблема: Автообновление не работает

**Симптомы:**
- Сертификат истёк
- Cron задача не выполняется

**Решение:**

1. **Проверьте cron:**
```bash
crontab -l | grep acme
```

2. **Если нет задачи:**
```bash
~/.acme.sh/acme.sh --install-cronjob
```

3. **Проверьте права:**
```bash
ls -la ~/.acme.sh/acme.sh
# Должен быть исполняемым: -rwxr-xr-x
```

4. **Проверьте логи cron:**
```bash
sudo grep acme /var/log/syslog
# или
sudo journalctl | grep acme
```

5. **Ручное обновление для теста:**
```bash
~/.acme.sh/acme.sh --cron --force
```

---

## Безопасность

### 1. Закрытие порта 80

После получения сертификата можно закрыть порт 80:

**⚠️ Внимание:** Порт 80 нужен для обновления сертификатов!

**Рекомендация:** Оставьте порт 80 открытым, но ограничьте доступ:

```bash
# UFW - разрешить только для ACME challenge
sudo ufw allow from any to any port 80 proto tcp comment "ACME only"

# Или используйте DNS API метод (тогда порт 80 не нужен)
```

### 2. Секретные пути панели

Измените путь к панели для скрытности:

1. Panel Settings → Panel URL Root Path
2. Измените `/` на `/secret-admin-123456/`
3. Сохраните

Теперь панель доступна только по: `https://your-domain.com:2053/secret-admin-123456/`

### 3. Ограничение доступа по IP

**Через firewall:**

```bash
# Разрешить только с вашего IP
sudo ufw delete allow 2053/tcp
sudo ufw allow from YOUR_IP to any port 2053 proto tcp

# Для нескольких IP
sudo ufw allow from 1.2.3.4 to any port 2053 proto tcp
sudo ufw allow from 5.6.7.8 to any port 2053 proto tcp
```

**Через nginx (требует дополнительной настройки):**

```nginx
server {
    listen 443 ssl;
    server_name your-domain.com;

    ssl_certificate /path/to/fullchain.pem;
    ssl_certificate_key /path/to/privkey.pem;

    location / {
        allow 1.2.3.4;    # Ваш IP
        deny all;
        proxy_pass http://127.0.0.1:2053;
    }
}
```

### 4. Изменение порта панели

Измените стандартный порт 2053 на нестандартный:

1. Panel Settings → Panel Port
2. Измените на, например, `9443` или `8888`
3. Обновите firewall:
```bash
sudo ufw delete allow 2053/tcp
sudo ufw allow 9443/tcp
```

### 5. Двухфакторная аутентификация

Если 3X-UI поддерживает 2FA - обязательно включите:

1. Panel Settings → Security
2. Enable Two-Factor Authentication
3. Сканируйте QR-код в Google Authenticator

---

## Примеры команд

### Проверка сертификатов

```bash
# Информация о сертификате
openssl x509 -in /home/sham/PhpstormProjects/myXray/data/cert/fullchain.pem -text -noout

# Дата истечения
openssl x509 -in /home/sham/PhpstormProjects/myXray/data/cert/fullchain.pem -noout -enddate

# Проверка что ключ соответствует сертификату
openssl x509 -in /home/sham/PhpstormProjects/myXray/data/cert/fullchain.pem -noout -modulus | md5sum
openssl rsa -in /home/sham/PhpstormProjects/myXray/data/cert/privkey.pem -noout -modulus | md5sum
# Должны совпадать
```

### Работа с acme.sh

```bash
# Список всех сертификатов
~/.acme.sh/acme.sh --list

# Информация о конкретном сертификате
~/.acme.sh/acme.sh --info -d your-domain.com

# Принудительное обновление
~/.acme.sh/acme.sh --renew -d your-domain.com --force

# Отзыв сертификата
~/.acme.sh/acme.sh --revoke -d your-domain.com

# Удаление сертификата
~/.acme.sh/acme.sh --remove -d your-domain.com

# Обновление acme.sh
~/.acme.sh/acme.sh --upgrade

# Включение auto-upgrade
~/.acme.sh/acme.sh --upgrade --auto-upgrade
```

### Тестирование SSL

```bash
# Проверка SSL соединения
curl -I https://your-domain.com:2053

# Детальная информация
openssl s_client -connect your-domain.com:2053 -servername your-domain.com

# Проверка цепочки сертификатов
openssl s_client -connect your-domain.com:2053 -showcerts

# SSL тест (онлайн)
# Откройте в браузере:
# https://www.ssllabs.com/ssltest/analyze.html?d=your-domain.com
```

### Backup сертификатов

```bash
# Резервная копия (уже включена в scripts/backup.sh)
cd /home/sham/PhpstormProjects/myXray
./scripts/backup.sh

# Ручное копирование
tar -czf certs-backup-$(date +%Y%m%d).tar.gz data/cert/

# Восстановление
tar -xzf certs-backup-20241225.tar.gz
```

---

## Дополнительные ресурсы

- **acme.sh документация:** https://github.com/acmesh-official/acme.sh
- **Let's Encrypt:** https://letsencrypt.org/
- **3X-UI GitHub:** https://github.com/MHSanaei/3x-ui
- **SSL Labs Test:** https://www.ssllabs.com/ssltest/

---

## Заключение

Теперь ваша админ-панель 3X-UI защищена SSL сертификатом от Let's Encrypt!

**Что дальше:**
- Настройте TLS для VPN протоколов в Inbounds
- Настройте регулярные бэкапы через cron
- Изучите дополнительные меры безопасности в [SECURITY.md](./SECURITY.md)
- Настройте мониторинг истечения сертификатов

**Если возникли проблемы:**
- Проверьте логи: `~/.acme.sh/acme.sh.log`
- Проверьте логи Docker: `docker logs 3x-ui`
- Проверьте раздел [Устранение неполадок](#устранение-неполадок)

**Удачи! 🔒**
